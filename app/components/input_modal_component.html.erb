<div class="z-20 fixed w-screen h-screen top-0 left-0 flex items-center justify-center" style="display: none" data-controller="input-modal">
  <input type="hidden" class="modal-id" value="<%= @modal_id %>">
  <div class="w-full h-full bg-black/[.45] fixed top-0 left-0 z-[19]" data-action="click->input-modal#closeModal keydown@window->input-modal#closeModal"></div>
  <div class="flex flex-col items-start justify-center z-20 w-[500px] gap-3">
    <p class="text-sm font-medium bg-background rounded px-2 py-1 shadow-xl z-20"><%= @label %></p>
    <input placeholder="<%= @placeholder %>" class="shadow-xl min-w-0 rounded-md
     border border-gray-200 placeholder-gray-500 focus:outline-none
      relative z-20 text-base leading-6 palette-input bg-background
       dark:bg-secondaryBg rounded-t-lg h-full w-full px-6 py-4 !border-0
        hover:border-transparent !focus:outline-none focus:border-transparent
         text-current" style="height: 56px;" data-input-modal-target="input" data-action="keydown->input-modal#submitForm">
  </div>
</div>